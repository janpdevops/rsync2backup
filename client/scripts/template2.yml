      - SUDO_ACCESS=false 
      - USER_NAME=#USER_NAME#
    volumes:
      - #SERVER_CONFIG_DIR#:/config
      - #SERVER_DATA_DIR#:/data
    ports:
      - #RSYNC_PORT#:2222
    restart: unless-stopped


